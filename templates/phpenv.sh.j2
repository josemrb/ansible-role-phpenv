{% if phpenv_env == 'system' %}
export PATH={{ phpenv_root }}/bin:$PATH
export PHPENV_ROOT="{{ phpenv_root }}"
eval "$(phpenv init -)"
{% else %}
if [ -d "$HOME/.phpenv" ]; then
  export PATH=$HOME/.phpenv/bin:$PATH;
  export PHPENV_ROOT=$HOME/.phpenv;
  eval "$(phpenv init -)";
fi
{% endif %}
